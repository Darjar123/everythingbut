{% if breadcrumb %}
    <ul>
    {% for item in breadcrumb %}
        {% if item.url %}
          	<li><a href="{{ item.url }}">{{ item.text }}</a> </li>
        {% else %}
          	<li>{{ item.text }}</li>
        {% endif %}
    {% endfor %}
    </ul>
{% endif %}
