{% set img_uri = product_entity.field_product_images.entity.uri.value %}
{% set img_url = file_url(img_uri) %}
{% set img_alt = product_entity.field_product_images.alt %}

{% set image600x526 = getUrlByImageStyle(img_uri, 'image600x526')  %}
{% set image600x796 = getUrlByImageStyle(img_uri, 'image600x796')  %}
{% set product_image_list = getUrlByImageStyle(img_uri, 'product_image_list')  %}
{% set class_col = 'col-md-4' %}


<div class="col-md-2 col-sm-6 col-12">
		<div class="shop-classic">
			<div class="shop-classic-img">
				<a href="{{ product_url }}"><img src="{{ product_image_list }}" alt="{{ img_alt }}"></a>
			</div>
			<div class="shop-classic-text">
				<h4><a href="{{ product_url }}">{{ product.title }}</a></h4>
				<h5>{{ product.field_subtitle }}</h5>
				<h6> <strong>{{ product.variation_price }}</strong></h6>
         {{ product.field_movies }}
			</div>
		</div>
	</div>
